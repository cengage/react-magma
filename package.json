{
  "name": "react-magma",
  "version": "0.1.0-SNAPSHOT",
  "private": true,
  "scripts": {
    "postinstall": "lerna bootstrap && npm run build",
    "test": "lerna run --scope react-magma-dom --scope react-magma-core --scope e2eTesting test",
    "build": "lerna run build",
    "bootstrap": "lerna bootstrap",
    "lint": "lerna run lint"
  },
  "husky": {
    "hooks": {
      "pre-commit": "lerna run lint && npm run test"
    }
  },
  "devDependencies": {
    "husky": "^1.0.0-rc.14",
    "lerna": "^3.1.4"
  }
}
