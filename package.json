{
  "name": "root",
  "private": true,
  "scripts": {
    "test": "lerna run test",
    "build": "lerna run build",
    "bootstrap": "lerna bootstrap",
    "docs": "lerna exec --scope react-magma-docs docz dev"
  },
  "devDependencies": {
    "lerna": "^3.1.4"
  }
}
