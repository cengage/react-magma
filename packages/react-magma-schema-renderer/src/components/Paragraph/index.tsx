import React from 'react';
import { Paragraph as MagmaParagraph, ParagraphProps } from 'react-magma-dom';

export const Paragraph = (props: ParagraphProps) => {
  return <MagmaParagraph {...props} />;
};
