---
title: 'Tooltips'
---

# Tooltip

## Basic usage

The `content` prop is a React Node, and determines the content inside of the tooltip bubble.

The `trigger` prop is a React Node, and determines the content that tiggers the tooltip on hover or on focus. Note: in order to trigger the content on focus, the `trigger` should be a focusable element.


```.jsx
    <Tooltip content="What is this?" id="basicTooltip" trigger={<Button icon={<QuestionCircleIcon />} variant="link" />} />
```

## position
The `Tooltip` component takes a `position` property, that accepts the following values: bottom, left, right, top. Bottom is the default value.

```.jsx
    <>
        <Tooltip position='bottom' content="Tooltip bottom" trigger={<Button ariaLabel="Question Icon" icon={<QuestionCircleIcon />} variant="link" />} />
        <Tooltip position='left' content="Tooltip left" trigger={<Button icon={<QuestionCircleIcon />} variant="link" />} />
        <Tooltip position='right' content="Tooltip right" trigger={<Button icon={<QuestionCircleIcon />} variant="link" />} />
        <Tooltip position='top' content="Tooltip top" trigger={<Button icon={<QuestionCircleIcon />} variant="link" />} />
    </>
```



## Tooltip Props

| Prop     | Type                                                     | Default  | Required |
| -------- | -------------------------------------------------------- | -------- | :------: |
| content  | node                                                     |          | &#10003; |
| position | enum: 'bottom' &#124; 'left' &#124; 'right' &#124; 'top' | 'bottom' |          |
| trigger  | node                                                     |          | &#10003; |