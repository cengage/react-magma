---
title: 'Inputs'
---

import DocsHeading from '../../components/docs-heading'

<DocsHeading to="/design/inputs/" type="design">
  Inputs
</DocsHeading>

Input fields are used in a number of ways for both collecting and displaying data from the user.

## Basic Usage

The label/input association will be handled via the passed in `id` if one is supplied or via an auto-generated `id` if not supplied.

```.jsx
    <div>
      <Input
          id='input1'
          labelText='Label'
          placeholder='This is a placeholder'
          required={false}
          value='Default Value' />
    </div>
```

## Password

```.jsx
  <>
    <Input
      id='inputPw'
      labelText='Password'
      placeholder='Enter your password'
      type='password' />
    <Input
      hidePasswordMaskButton
      id='inputPwWithNoMask'
      labelText='Password With No Unmask Button'
      placeholder='Enter your password'
      type='password' />
  </>
```

## Number

```.jsx
    <Input
        id='inputNum'
        labelText='Number'
        placeholder='Enter a number'
        type='number' />
```

## Disabled

```.jsx
    <Input
        id='inputDisabled'
        labelText='Disabled'
        placeholder='The input is disabled'
        disabled />
```

## Error Message

If an input has an `errorMessage`, the input will be styled to highlight it's error state and the error message will appear below the input field.
If an error message is present, it will replace the helper text.

```.jsx
    <Input
        id='inputError'
        errorMessage='Please correct this error'
        helperMessage='Helper text'
        labelText='Error Message'
        placeholder='Type in here' />
```

## Helper Message

The `helperMessage` appears underneath the input field. It will not appear if an error message is present.

```.jsx
    <Input
        id='inputHelper'
        helperMessage='Helper text goes here'
        labelText='Helper Message'
        placeholder='Type in here' />
```

## Input with Icon

To include an icon both the `icon` and `iconPosition` props must be passed in. Icon position can either be `left` or `right`.

```.jsx
    <div>
      <Input
        iconPosition='left'
        id='inputIcon'
        labelText='Input with Icon Left'
        placeholder='Type in here'
        icon={<CalendarIcon />} />

      <Input
        iconPosition='right'
        id='inputIconRight'
        labelText='Input with Icon Right'
        placeholder='Type in here'
        icon={<Search2Icon />} />
    </div>
```

## Input Sizes

The `inputSize` prop can be used to control the input size. Options are `small`, `medium` and `large`, with `medium` being the default.

```.jsx
    <div>
      <Input
        iconPosition='right'
        id='inputSmall'
        inputSize='small'
        labelText='Small Input'
        placeholder='Type in here'
        icon={<Search2Icon />} />

      <Input
        iconPosition='right'
        id='inputMed'
        labelText='Medium Input'
        placeholder='Type in here'
        icon={<Search2Icon />} />
      <Input
        iconPosition='right'
        id='inputLarge2'
        inputSize='large'
        labelText='Large Input'
        placeholder='Type in here'
        icon={<Search2Icon />} />
      <Input
          errorMessage='Error'
          id='inputSmallErr'
          inputSize='small'
          labelText='Small Input with Error'
          placeholder='Type in here' />
      <Input
          errorMessage='Error'
          id='inputMedErr'
          labelText='Medium Input with Error'
          placeholder='Type in here' />
      <Input
          errorMessage='Error'
          id='inputLargeErr'
          inputSize='large'
          labelText='Large Input with Error'
          placeholder='Type in here' />
    </div>
```

## Inverse inputs

Inverse is an optional boolean prop, that may be passed in when inputs are to be displayed on a dark background.

```.jsx
    <div style={{ background : '#00263E', padding: '10px' }}>
      <Input
          id='inputInverse'
          inverse
          labelText='Inverse Input'
          placeholder='Type in here' />

      <Input
          helperMessage='Helper text'
          id='inputInverseHelper'
          inverse
          labelText='Inverse Input with Helper Text'
          placeholder='Type in here' />

      <Input
          errorMessage='Please correct your error'
          id='inputInverseError'
          inverse
          labelText='Inverse Input with Error Message'
          placeholder='Type in here' />
    </div>
```

## Input with hidden label

The `labelVisuallyHidden` prop can be used to visually hide the text label, while still making the text available to screen readers.

```.jsx
    <Input
      iconPosition='right'
      id='inputLabelHidden'
      labelVisuallyHidden
      labelText='Search'
      placeholder='Search'
      icon={<Search2Icon />} />
```

## Multi-line (textarea)

The optional `multiline` boolean prop will cause the component to render a textarea, rather than a text input.

```.jsx
    <Input
        id='textarea'
        labelText='Multi-line Textarea'
        multiline
        placeholder='Type in here' />
```

## Change Events

```.jsx
  class ChangedInput extends React.Component {
    constructor(props) {
      super(props)
      this.state = {
        message: ''
      };

      this.changeValueMessage = this.changeValueMessage.bind(this);
    }

    changeValueMessage(event) {
      this.setState({message: event.target.value})
    }

    render() {
      return <div>
        <p><strong>Changed Value: </strong> <span id="changedValue">{this.state.message}</span></p>
        <Input
          id='inputChanged'
          labelText='Test Value'
          value=''
          placeholder='Type in here'
          onChange={this.changeValueMessage} />
      </div>
    }
  }
```

## Focus Events

```.jsx
  class FocusedInput extends React.Component {
    constructor(props) {
      super(props)
      this.state = {
        focused: false,
        message: 'No'
      };

      this.changeFocusedMessage = this.changeFocusedMessage.bind(this);
    }

    changeFocusedMessage() {
      const focused = !this.state.focused;

      this.setState(state => ({
        focused,
        message: focused ? 'Yes' : 'No',
      }));
    }

    render() {
      return <div>
        <p><strong>Focused: </strong> <span id="focused">{this.state.message}</span></p>
        <Input
          id='inputFocused'
          labelText='Focused?'
          onFocus={this.changeFocusedMessage}
          onBlur={this.changeFocusedMessage} />
      </div>
    }
  }
```

## Forward Ref

Using React's `forwardRef` feature you can gain access to the reference of the internal input.

```.jsx
  class Example extends React.Component {
      constructor(props) {
        super(props)
        this.inputRef = React.createRef();
      }

      render() {
        return <div>
        <Input
          ref={this.inputRef}
          labelText='Input to be focused' />

        <Button
          onClick={() => {
            this.inputRef.current.focus();
          }}>Click to focus input
        </Button>
        </div>
      }
    }
```

## Testing

Passing in the `testId` prop will add the `data-testid` attribute to the input element for easier querying in tests.

```html
<div>
  <label for="input1">Label</label>
  <div>
    <input
      id="input1"
      data-testid="test-id"
      placeholder="This is a placeholder"
      type="text"
      value="Default Value"
    />
  </div>
</div>
```

## Input Props

| Prop                   | Type                                                | Default  | Required |
| ---------------------- | --------------------------------------------------- | -------- | :------: |
| autoFocus              | boolean                                             |          |
| disabled               | boolean                                             |          |
| errorMessage           | string                                              |          |
| helperMessage          | string                                              |          |
| hidePasswordMaskButton | boolean                                             |          |
| icon                   | string                                              |          |
| iconPosition           | enum: 'left' &#124; 'right' &#124;                  | 'left'   |
| id                     | string                                              |          | &#10003; |
| inputSize              | enum: 'small' &#124; 'medium' &#124; 'large' &#124; | 'medium' |
| inputStyle             | React.CSSProperties                                 |          |
| inverse                | boolean                                             |          |
| labelStyle             | React.CSSProperties                                 |          |
| labelText              | string                                              |          | &#10003; |
| labelVisuallyHidden    | boolean                                             |          |
| multiline              | boolean                                             |          |
| onBlur                 | function                                            |          |
| onChange               | function                                            |          |
| onFocus                | function                                            |          |
| placeholder            | string                                              |          |
| ref                    | React.Ref                                           |          |
| required               | boolean                                             |          |
| testId                 | string                                              |          |
| type                   | enum: 'text' &#124; 'password' &#124; 'number'      | 'text'   |
| value                  | string                                              |          |
| style                  | React.CSSProperties                                 |          |
