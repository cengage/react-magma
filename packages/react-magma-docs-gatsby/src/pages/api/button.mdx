---
title: "Buttons"
---

# Buttons

**Buttons are clickable items used to perform an action.**

Buttons should be used in situations where users might need to:

* Submit a form.
* Begin a new task.
* Trigger a new UI element to appear on the page.
* Specify a new or next step in a process.

## Sizes

```.jsx
    <p>
        <Button size="small" text="Small" />
        <Button text="Medium (default)" />
        <Button size="large" text="Large" />
    </p>
```

## Colors

```.jsx
    <p>
        <Button color="primary" text="Primary (default)" />
        <Button color="secondary" text="Secondary" />
        <Button color="success" text="Success" />
        <Button color="danger" text="Danger" />
    </p>
```

## Types

```.jsx
    <div>
      <p>
          <Button color="primary" type="solid" text="Solid (default)" />
          <Button color="primary" type="outline" text="Outline" />
          <Button color="primary" type="link" text="Link" />
      </p>
      <p>
          <Button color="secondary" type="solid" text="Solid (default)" />
          <Button color="secondary" type="outline" text="Outline" />
          <Button color="secondary" type="link" text="Link" />
      </p>
      <p>
          <Button color="success" type="solid" text="Solid (default)" />
          <Button color="success" type="outline" text="Outline" />
          <Button color="success" type="link" text="Link" />
      </p>
      <p>
          <Button color="danger" type="solid" text="Solid (default)" />
          <Button color="danger" type="outline" text="Outline" />
          <Button color="danger" type="link" text="Link" />
      </p>
    </div>
```

## Shapes

```.jsx
    <p>
        <Button color="primary" text="Fill (default)" />
        <Button color="primary" shape="leftCap" text="Left Cap" />
        <Button color="primary" shape="rightCap" text="Right Cap" />
    </p>
```

## Inverse

```.jsx
    <div style={{ background : '#00263E', padding: '10px' }}>
      <p>
          <Button color="primary" inverse text="Solid inverse" />
          <Button color="secondary" inverse text="Solid inverse" />
          <Button color="success" inverse text="Solid inverse" />
          <Button color="danger" inverse text="Solid inverse" />
      </p>
      <p>
          <Button color="primary" type="outline" inverse text="Outline inverse" />
          <Button color="secondary" type="outline"  inverse text="Outline inverse" />
          <Button color="success" type="outline"  inverse text="Outline inverse" />
          <Button color="danger" type="outline"  inverse text="Outline inverse" />
      </p>
      <p>
          <Button color="primary" type="link" inverse text="Link inverse" />
          <Button color="secondary" type="link"  inverse text="Link inverse" />
          <Button color="success" type="link"  inverse text="Link inverse" />
          <Button color="danger" type="link"  inverse text="Link inverse" />
      </p>
      <p>
          <Button disabled text="Disabled inverse" />
          <Button disabled type="outline" text="Disabled outline inverse" />
          <Button disabled type="link" text="Disabled link inverse" />
      </p>
    </div>
```

## All Caps

```.jsx
    <p>
        <Button color="primary" allCaps text="All caps" />
        <Button color="primary" allCaps={false} text="All caps false" />
    </p>
```

## Disabled

```.jsx
    <p>
        <Button disabled text="Disabled" />
        <Button disabled type="outline" text="Disabled" />
        <Button disabled type="link" text="Disabled" />
    </p>
```

## Event Handling

```.jsx
  class Example extends React.Component {
      constructor(props) {
        super(props)
        this.state = {
          counter: 0
        }
      }

      render() {
        return <div>
        <p><strong>Counter: </strong> <span id="counter">{this.state.counter}</span></p>
        <Button
          handleClick={() => {
            this.setState(state => ({counter: state.counter + 1}))
          }}
          text="Click me" />
        </div>
      }
    }
```
