import React from 'react';
import { Banner } from '.';

export default {
  component: Banner,
  title: 'Banner',
};

export const Default = () => {
  return <Banner>Default (info) banner</Banner>;
};
