---
pageTitle: Tooltip API
title: Tooltip
---

import { ScopeableLayout } from '../../components/layout';
import {
  CircleDownIcon,
  CircleLeftIcon,
  CircleRightIcon,
  CircleUpIcon,
  QuestionCircleIcon,
} from 'react-magma-icons';
import { Tooltip, IconButton } from 'react-magma-dom';
import { SimplePropsTable } from '../../components/props-table';
import DocsHeading from '../../components/docs-heading';

<DocsHeading to="/design/tooltips/" type="design">
  Tooltip
</DocsHeading>

## Basic Usage

The children of the tooltip, determines the content inside of the tooltip bubble.

The `trigger` prop must take a react element (not just a string), and determines the content that tiggers the tooltip on hover or on focus. Note: in order to trigger the content on focus (a requirement for accessibility), the `trigger` should be a focusable element.

```.jsx
  () => {
    const tooltipNodeContent = <>Lorem ipsum <i>dolor sit amet</i>, mutat utroque. Vel molestie invidunt dissentiet no, ut vim iriure.</>

    return (
      <>
          <Tooltip content="What's this?">
            <IconButton aria-label="Get Help" icon={<QuestionCircleIcon />} variant="link" />
          </Tooltip>

          <Tooltip content={tooltipNodeContent}>
            <IconButton aria-label="Get Help" icon={<QuestionCircleIcon />} variant="link" />
          </Tooltip>
      </>
    )
  }
```

## Position

The `Tooltip` component takes a `position` property, that accepts the following values: bottom, left, right, top. Top is the default value.

```.jsx
    <>
        <Tooltip position='bottom' content='Tooltip bottom'>
          <IconButton aria-label="Bottom" icon={<CircleDownIcon />} variant="link" />
        </Tooltip>

        <Tooltip position='left' content='Tooltip left'>
          <IconButton aria-label="Left" icon={<CircleLeftIcon />} variant="link" />
        </Tooltip>

        <Tooltip position='right' content='Tooltip right'>
          <IconButton aria-label="Right" icon={<CircleRightIcon />} variant="link" />
        </Tooltip>

         <Tooltip position='top' content='Tooltip top'>
          <IconButton aria-label="Top" icon={<CircleUpIcon />} variant="link" />
        </Tooltip>
    </>
```

## Inverse

The `isInverse` property is an optional boolean, that reverses the colors so that the component can better appear on a dark background.
The default value is `false`.

```.jsx
    <div style={{ background: '#00263E', color: '#fff'}}>
        <Tooltip isInverse position='right' content='Inverse tooltip'>
          <IconButton aria-label="Get Help" icon={<QuestionCircleIcon />} isInverse variant="link" />
        </Tooltip>
    </div>
```

## Custom Styles

The containing div for the tooltip, and the tooltip component itself can have custom styles applied to it, using the `containerStyle` and `tooltipStyle` props respectively.

```.jsx
        <Tooltip content='Styled tooltip' containerStyle={{background: 'pink', padding: '10px' }} tooltipStyle={{ border: '1px solid red', background: 'yellow', color: 'black', padding: '10px' }}>
          <IconButton aria-label="Get Help" icon={<QuestionCircleIcon />} variant="link" />
        </Tooltip>
```

## Tooltip Props

**Any other props supplied will be provided to the `div` element wrapping the tooltip content.**

<SimplePropsTable
  propertyValues={{
    children: {
      type: {
        name: 'node',
      },
      required: true,
      description:
        'The element that triggers the tooltip when it is hovered or focused.  Must be a react element (not a string) and should be a focusable element to meet a11y requirements',
    },
    containerStyle: {
      type: {
        name: 'React.CSSProperties',
      },
      required: false,
      description: 'Style properties for the component container element',
    },
    content: {
      type: {
        name: 'element',
      },
      required: true,
      description: 'The content of the tooltip',
    },
    isInverse: {
      type: {
        name: 'boolean',
      },
      required: false,
      description:
        'If true, the component will have inverse styling to better appear on a dark background',
      defaultValue: 'false',
    },
    position: {
      type: {
        name: 'enum',
        options: [
          'EnumTooltipPosition.bottom',
          'EnumTooltipPosition.left',
          'EnumTooltipPosition.right',
          'EnumTooltipPosition.top',
        ],
      },
      required: false,
      description: 'Position the tooltip appears in relation to its trigger',
      defaultValue: 'top',
    },
    testId: {
      type: {
        name: 'string',
      },
      required: false,
      description:
        'Test ID attached to an internal element as `data-testid` for consumer testing',
    },
    tooltipStyle: {
      type: {
        name: 'React.CSSProperties',
      },
      required: false,
      description: 'Style properties for the tooltip',
    },
  }}
/>

export default props => (
  <ScopeableLayout
    {...props}
    components={{
      Tooltip,
      IconButton,
      CircleDownIcon,
      CircleLeftIcon,
      CircleRightIcon,
      CircleUpIcon,
      QuestionCircleIcon,
    }}
  />
);
