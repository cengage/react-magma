---
title: 'Hide at Breakpoint'
---

import { ScopeableLayout } from '../../components/layout'
import { magma, Card, CardBody, HideAtBreakpoint } from 'react-magma-dom'
import { SimplePropsTable } from '../../components/props-table'

# Hide at Breakpoint

The `HideAtBreakpoint` component can be used to quickly hide a component at a particular breakpoint.
It takes two props, `minWidth` and `maxWidth` for the minimum and maximum widths, specified in pixels.

## Basic Usage

```.jsx
    <>
     <HideAtBreakpoint maxWidth={599}>
            <Card isInverse background={magma.colors.foundation02}>
              <CardBody><strong>Max-Width:</strong> This text will be visible when the browser width is greater than or equal to 600px.</CardBody>
            </Card><br/>
        </HideAtBreakpoint>
        <HideAtBreakpoint minWidth={600} background={magma.colors.pop01}>
             <Card isInverse background={magma.colors.pop01}>
              <CardBody><strong>Min-Width:</strong> This text will be visible when the browser width is less than 600px.</CardBody>
        </Card><br/>
        </HideAtBreakpoint>
        <HideAtBreakpoint>
             <Card isInverse background={magma.colors.success01}>
              <CardBody>This text will always be visible.</CardBody>
             </Card>
        </HideAtBreakpoint>
    </>
```

## Implementation

The `implementation` prop can be used to determine whether the browser uses CSS or JavaScript to determine what is shown.  
When `css` is used, the hidden content is simply hidden using CSS `display: none`. When `js` is used, the content to be hidden does not render in the DOM.  
The default implementation is `js`.

```.jsx
    <>
     <HideAtBreakpoint maxWidth={599} implemenation='css'>
            <Card isInverse background={magma.colors.foundation02}>
              <CardBody><strong>Max-Width:</strong> This text will be visible when the browser width is greater than or equal to 600px.</CardBody>
            </Card><br/>
        </HideAtBreakpoint>
        <HideAtBreakpoint minWidth={600} background={magma.colors.pop01} implemenation='css'>
             <Card isInverse background={magma.colors.pop01}>
              <CardBody><strong>Min-Width:</strong> This text will be visible when the browser width is less than 600px.</CardBody>
        </Card><br/>
        </HideAtBreakpoint>
    </>
```

## Hide at Breakpoint Props

**Any other props supplied will be provided to the wrapping `div` element.**

<SimplePropsTable
  propertyValues={{
    children: {
      type: {
        name: 'node',
      },
      required: true,
      description: 'The content of the component',
    },
    implemenation: {
      type: {
        name: 'enum',
        options: [
          'HideAtBreakpointImplementation.css',
          'HideAtBreakpointImplementation.js',
        ],
      },
      required: false,
      description:
        'Determins whether the component uses CSS or JavaScript to hide/show content',
      defaultValue: 'js',
    },
    maxWidth: {
      type: {
        name: 'number',
      },
      required: false,
      description: 'Maximum width at which to hide the content',
    },
    minWidth: {
      type: {
        name: 'number',
      },
      required: false,
      description: 'Minimum width at which to hide the content',
    },
  }}
/>

export default props => (
  <ScopeableLayout
    {...props}
    components={{ magma, Card, CardBody, HideAtBreakpoint }}
  />
)
