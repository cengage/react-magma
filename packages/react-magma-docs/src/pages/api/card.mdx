---
title: 'Cards'
---

import { ScopeableLayout } from '../../components/layout'
import { Card, CardBody, CardHeading } from 'react-magma-dom'
import { SimplePropsTable } from '../../components/props-table'

# Card

The `Card` component is relatively simple container. It does not have any margin or padding.  
The `CardBody` component can be placed inside, and will give the content padding.

## Basic Usage

```.jsx
        <Card>
            <CardBody>
                Some content
            </CardBody>
        </Card>
```

## Heading

Any content can be included in a card. To maintain a consistent heading style, the `CardHeading` component may be used.

```.jsx
        <Card>
            <CardHeading>
                Card heading
            </CardHeading>
            <CardBody>
                Some content
            </CardBody>
        </Card>
```

## Drop Shadow

Passing in the `hasDropShadow` prop will cause the `Card` componet to render with a box-shadow.

```.jsx
        <Card hasDropShadow>
            <CardBody>
                Some content
            </CardBody>
        </Card>
```

## Alignment

By default, content aligns to the left. However, the `align` prop can be specified, and it takes values `center`, `left`, and `right`.

```.jsx
    <>
        <Card align='right'>
            <CardHeading>
                Card heading
            </CardHeading>
            <CardBody>
                Some content
            </CardBody>
        </Card>
        <p></p>
        <Card align='center'>
            <CardHeading>
                Card heading
            </CardHeading>
            <CardBody>
                Some content
            </CardBody>
        </Card>
    </>
```

## Callout Type

The `cardCalloutType` prop takes values `danger`, `primary`, `success` and `warning`. If this prop is specified, the card will be styled
with a left border of the appropriate color.

```.jsx
    <>
        <Card calloutType='success'>
            <CardHeading>
                Card heading
            </CardHeading>
            <CardBody>
                Some content
            </CardBody>
        </Card>
        <p></p>
        <Card calloutType='danger'>
            <CardHeading>
                Card heading
            </CardHeading>
            <CardBody>
                Some content
            </CardBody>
        </Card>
    </>
```

## Width

By default, the card width is set to auto, but you can pass in another value to be set in the CSS.

```.jsx
    <>
        <Card width='200px'>
            <CardBody>
                Some content 200px wide
            </CardBody>
        </Card>
        <p></p>
        <Card width='50%'>
            <CardBody>
                Some content 50% wide
            </CardBody>
        </Card>
    </>
```

## Image in Card

To get an image to take up the full width of a card, without padding, place it outside of the `CardBody` component.

```.jsx
        <Card width='250px'>
            <img src="http://placekitten.com/250/125" />
            <CardBody>
                Some content
            </CardBody>
        </Card>
```

<SimplePropsTable
  props={{
    align: {
      type: {
        name: 'enum',
        options: [
          'CardAlignment.center',
          'CardAlignment.left',
          'CardAlignment.right',
        ],
      },
      required: false,
      description: 'Sets the alignment of the card content',
      defaultValue: 'left',
    },
    calloutType: {
      type: {
        name: 'enum',
        options: [
          'CardCalloutType.danger',
          'CardCalloutType.primary',
          'CardCalloutType.success',
          'CardCalloutType.warning',
        ],
      },
      required: false,
      description:
        'If a value is passed, the card will be styled as a callout for the specified type.',
      defaultValue: 'none',
    },
    children: {
      type: {
        name: 'node',
      },
      required: true,
      description: 'The content of the component',
    },
    hasDropShadow: {
      type: {
        name: 'boolean',
      },
      required: false,
      description: 'If true, card will render with a box-shadow',
      defaultValue: 'false',
    },
    width: {
      type: {
        name: 'string',
      },
      required: false,
      description: 'Width of the component, set by CSS.',
      defaultValue: 'auto',
    },
  }}
/>

export default props => (
  <ScopeableLayout {...props} components={{ Card, CardBody, CardHeading }} />
)
