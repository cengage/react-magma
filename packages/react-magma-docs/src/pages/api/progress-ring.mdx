---
title: 'Progress Ring'
---

import { ScopeableLayout } from '../../components/layout'
import { Button, ProgressRing, magma } from 'react-magma-dom'
import { SimplePropsTable } from '../../components/props-table'

# Progress Ring

## Basic Usage

```.jsx
  (props) =>{
    const [isActive, setIsActive] = React.useState(false);

    function toggleTimer() {
      setIsActive(!isActive)
    }

    return (
      <>
        <Button onClick={toggleTimer}>{isActive ? 'Pause' : 'Start'} Timer</Button>
        <ProgressRing isActive={isActive} />
      </>
    )}
```

## Custom Appearance

```.jsx
    <ProgressRing color={magma.colors.success01} size={50} strokeWidth={5} />
```

## Progress Ring Props

**Any other props supplied will be provided to the wrapping `div` element.**

<SimplePropsTable
  propertyValues={{
    color: {
      type: {
        name: 'string',
      },
      required: false,
      description: 'Color of the ring outline',
      defaultValue: 'primary',
    },
    percentage: {
      type: {
        name: 'number',
      },
      required: false,
      description: 'The percentage of which the ring is filled',
      defaultValue: '0',
    },
    size: {
      type: {
        name: 'number',
      },
      required: false,
      description: 'Radius of the ring in pixels',
      defaultValue: '30',
    },
    strokeWidth: {
      type: {
        name: 'number',
      },
      required: false,
      description: 'Thickness of the ring outline in pixels',
      defaultValue: '3',
    },
    testId: {
      type: {
        name: 'string',
      },
      required: false,
      description:
        'Test ID attached to an internal element as `data-testid` for consumer testing',
    },
  }}
/>

export default props => (
  <ScopeableLayout {...props} components={{ Button, ProgressRing, magma }} />
)
