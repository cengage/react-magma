---
title: 'Password Inputs'
---

import { ScopeableLayout } from '../../components/layout'
import { PasswordInput, Button } from 'react-magma-dom'
import { Link } from 'gatsby'
import { SimplePropsTable } from '../../components/props-table'

# Password Input

The PasswordInput is a variation of the Input component. Please see the <Link to='/api/input'>Input documenation</Link> for more examples and information.

## Basic Usage

By default, users can show the password, by pressing a toggle button.

```.jsx
    <PasswordInput
      labelText='Password' />
```

## Hide Passwork Mask Button

To prevent users from showing the password, the optional boolean `isPasswordMaskButtonHidden` can be used.

```.jsx
    <PasswordInput
      isPasswordMaskButtonHidden
      labelText='Password With No Unmask Button' />
```

## Custom Strinigs

The following option props can be used to override various text strings that appear on the UI or to users using assistive technology: `hiddenPasswordAnnounceText`, `hidePasswordButtonAriaLabel`, `hidePasswordButtonText`, `shownPasswordAnnounceText`, `showPasswordButtonAriaLabel`, `showPasswordButtonText`.

```.jsx
    <PasswordInput
      labelText='Password'
      hiddenPasswordAnnounceText='Custom message'
      hidePasswordButtonAriaLabel='Custom hide button aria-label'
      hidePasswordButtonText='Conceal'
      shownPasswordAnnounceText='Another custom message'
      showPasswordButtonAriaLabel='Custom show button aria-label'
      showPasswordButtonText='Reveal'
    />

```

## Disabled

```.jsx
    <PasswordInput
        labelText='Disabled'
        disabled />
```

## Input Size

The `inputSize` prop can be used to control the input size. Options are `medium` and `large`, with `medium` being the default.

```.jsx
      <PasswordInput
        inputSize='large'
        labelText='Large Password' />

```

## Inverse

`isInverse` is an optional boolean prop, that may be passed in when inputs are to be displayed on a dark background.

```.jsx
    <div style={{ background : '#00263E', padding: '20px' }}>
      <PasswordInput
          helperMessage='Helper text'
          isInverse
          labelText='Inverse Input' />
    </div>
```

## Password Input Props

**Any other props supplied will be provided to the `input` element.**

Many of the PasswordInput props are shared with the Input component. Please see the <Link to='/api/input'>Input documenation</Link> for more examples and information.

<SimplePropsTable
  propertyValues={{
    containerStyle: {
      type: {
        name: 'React.CSSProperties',
      },
      required: false,
      description: 'Style properties for the component container element',
    },
    defaultValue: {
      type: {
        name: 'string',
      },
      required: false,
      description: 'Default value for the form field',
      defaultValue: '',
    },
    disabled: {
      type: {
        name: 'boolean',
      },
      required: false,
      description: 'If true, element is disabled',
      defaultValue: 'false',
    },
    errorMessage: {
      type: {
        name: 'string',
      },
      required: false,
      description:
        'Text of the error message.  If a value is provided, the input will be styled as an error state and the error message will display.',
      defaultValue: '',
    },
    helperMessage: {
      type: {
        name: 'string',
      },
      required: false,
      description: 'Text of the helper message.',
      defaultValue: '',
    },
    hiddenPasswordAnnounceText: {
      type: {
        name: 'string',
      },
      required: false,
      description: 'Text read by screen reader when the password is hidden',
      defaultValue: 'Password is now hidden',
    },
    hidePasswordButtonAriaLabel: {
      type: {
        name: 'string',
      },
      required: false,
      description: 'Aria-label for the "Hide Password" button',
      defaultValue: 'Hide password',
    },
    hidePasswordButtonText: {
      type: {
        name: 'string',
      },
      required: false,
      description: 'Text displayed on screen for the "Hide Password" button',
      defaultValue: 'Hide',
    },
    inputSize: {
      type: {
        name: 'enum',
        options: ['InputSize.large', 'InputSize.medium'],
      },
      required: false,
      description: 'Relative size of the component',
      defaultValue: 'medium',
    },
    inputStyle: {
      type: {
        name: 'React.CSSProperties',
      },
      required: false,
      description: 'Style properties for the input element',
    },
    isInverse: {
      type: {
        name: 'boolean',
      },
      required: false,
      description:
        'If true, the component will have inverse styling to better appear on a dark background',
      defaultValue: 'false',
    },
    isLabelVisuallyHidden: {
      type: {
        name: 'boolean',
      },
      required: false,
      description:
        'If true, label text will be hidden visually, but will still be read by assistive technology',
      defaultValue: 'false',
    },
    isPasswordMaskButtonHidden: {
      type: {
        name: 'boolean',
      },
      required: false,
      description:
        'If true, label text will be hidden visually, but will still be read by assistive technology',
      defaultValue: 'false',
    },
    labelStyle: {
      type: {
        name: 'React.CSSProperties',
      },
      required: false,
      description: 'Style properties for the label element',
    },
    labelText: {
      type: {
        name: 'string',
      },
      required: true,
      description: 'Text for label',
    },
    placeholder: {
      type: {
        name: 'string',
      },
      required: false,
      description: 'Text for input placeholder',
      defaultValue: 'Search',
    },
    required: {
      type: {
        name: 'boolean',
      },
      required: false,
      description: 'If true, the form field is required',
      defaultValue: 'false',
    },
    shownPasswordAnnounceText: {
      type: {
        name: 'string',
      },
      required: false,
      description: 'Text read by screen reader when the password is hidden',
      defaultValue: 'Password is now hidden',
    },
    showPasswordButtonAriaLabel: {
      type: {
        name: 'string',
      },
      required: false,
      description: 'Aria-label for the "Show Password" button',
      defaultValue:
        'Show password. Note: this will visually expose your password on the screen',
    },
    showPasswordButtonText: {
      type: {
        name: 'string',
      },
      required: false,
      description: 'Text displayed on screen for the "Show Password" button',
      defaultValue: 'Show',
    },
    testId: {
      type: {
        name: 'string',
      },
      required: false,
      description:
        'Test ID attached to an internal element as `data-testid` for consumer testing',
    },
    value: {
      type: {
        name: 'string',
      },
      required: false,
      description: 'Value for the form field',
      defaultValue: '',
    },
  }}
/>

export default props => (
  <ScopeableLayout {...props} components={{ PasswordInput, Button }} />
)
