---
pageTitle: useMediaQuery Hook API
title: useMediaQuery Hook
---

import { ScopeableLayout } from '../../components/layout';
import { magma, useMediaQuery, Button } from 'react-magma-dom';
import DocsHeading from '../../components/docs-heading';

<DocsHeading type="api">useMediaQuery Hook</DocsHeading>

The `useMediaQuery` custom hook can be used to set a prop based on screen size.

```.jsx
() => {
  const isSmallerScreen = useMediaQuery(`(max-width:${magma.breakpoints.large}px)`);

  return (
    <Button isFullWidth={isSmallerScreen}>Full Width at Small Screens</Button>
  )
}
```

export default props => (
  <ScopeableLayout
    {...props}
    components={{
      magma,
      useMediaQuery,
      Button,
    }}
  />
);
