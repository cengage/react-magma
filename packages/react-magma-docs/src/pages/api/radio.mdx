---
pageTitle: Radio Button API
title: Radio Button
---

import { ScopeableLayout } from '../../components/layout';
import { Alert, Radio, RadioGroup, Button, magma } from 'react-magma-dom';
import DocsHeading from '../../components/docs-heading';
import { Link } from 'gatsby';
import { SimplePropsTable } from '../../components/props-table';

<DocsHeading to="/design/selection-controls" type="design">
  Radio Button
</DocsHeading>

## Basic Usage

Radio buttons are form elements used to select one item from a set of mutually exclusive options. The `RadioGroup` component is used as the container for the set of options. The `Radio` component is used for each of the options themselves.

See also: <Link to="/api/checkbox">Checkboxes</Link>, <Link to="/api/toggle">Toggle Switches</Link>, and the <Link to="/design/selection-controls">Design Guidelines</Link> for selection controls in general.

The label/radio association will be handled via the passed in `id` if one is supplied or via an auto-generated `id` if not supplied.

```.jsx
  <RadioGroup labelText='Basic Usage' id='basicGroup' name='basic'>
    <Radio
        id='radio1'
        labelText='Option one label'
        value='1' />

    <Radio
        id='radio2'
        labelText='Option two label is really long and can wrap to multiple lines lorem ipsum dolar sit amet is really long and can wrap to multiple lines'
        value='2' />
    </RadioGroup>
```

## Default Selected Value

To set a selected value by default, use the `value` prop on the `RadioGroup` to pass in the `value` of the option you wish to select.

```.jsx
  <RadioGroup labelText='Default Selected Value' id='defaultSelectedGroup' name='defaultSelected' value='selectedOption'>
    <Radio
        id='radioSelected'
        labelText='Selected option'
        value='selectedOption' />

    <Radio
        id='radioUnselected'
        labelText='Unselected option'
        value='unselectedOption' />
    </RadioGroup>
```

## Colors

By default, the radio buttons use the theme's primary color. But any other color may be passed in.

```.jsx
  <RadioGroup labelText='Colors' id='colorsGroup' name='colors'>
    <Radio
        id='colorRadio'
        labelText='Default color'
        value='default' />
      <Radio
          color={magma.colors.success01}
          id='successColorRadio'
          labelText='Success color'
          value='success' />
      <Radio
          color={magma.colors.danger}
          id='dangerColorRadio'
          labelText='Danger color'
          value='danger' />
      <Radio
          color={magma.colors.pop02}
          id='popColorRadio'
          labelText='Pop color'
          value='pop' />
    </RadioGroup>
```

## Disabled

```.jsx
  <RadioGroup labelText='Disabled Radio Buttons' id='disabledGroup' name='disabled'>
    <Radio
        disabled
        id='disabledRadio'
        labelText='Disabled radio 1'
        value='1' />
    <Radio
        disabled
        id='disabledRadio2'
        labelText='Disabled radio 2'
        value='2' />
    </RadioGroup>
```

## Helper Message

The `helperMessage` appears underneath the group of radio buttons. It will not appear if an error message is present. Can be a node or a string.

```.jsx
<RadioGroup
      helperMessage="Your choice of color is important"
      labelText="Choose a color"
      name="helperMessage">
      <Radio
          labelText='Red'
          value='R' />
      <Radio
          labelText='Blue'
          value='B' />
      <Radio
          labelText='Yellow'
          value='Y' />
    </RadioGroup>
```

## Required Fields / Error Message

The `required` attribute can be indicated to the `RadioGroup` component if a selection is required.
You will also need to indicate the required field to the user in the UI.

If a radio group has an `errorMessage`, the radio buttons will be styled to highlight the error state and the error message will appear below the fields.
If an error message is present, it will replace the helper text. Can be a node or a string.

```.jsx
 (props) => {

  const [hasError, setHasError] = React.useState(false);
  const [requiredFieldsValue, setRequiredFieldsValue] = React.useState('');

  function changeValue(e) {
    setRequiredFieldsValue(e.target.value);
  }

  function submit(){
    if (requiredFieldsValue === '') {
      setHasError(true);
    }
    else {
      setHasError(false);
    }
  }

  function reset(){
    setHasError(false);
    setRequiredFieldsValue('');
  }

   return(
     <>
      <p>* indicates required fields</p>
      <RadioGroup
        errorMessage={hasError ? "Please select a color" : null}
        required
        labelText='Choose a color *'
        name='requiredFields'
        onChange={changeValue}
        value={requiredFieldsValue}>
        <Radio
            labelText='Red'
            value='R' />
        <Radio
            labelText='Blue'
            value='B' />
        <Radio
            labelText='Yellow'
            value='Y' />
      </RadioGroup>
      <br />
      <Button onClick={submit}>Submit</Button>
      <Button onClick={reset} color="secondary">Reset</Button>
    </>
  )
 }
```

## Inverse

The `isInverse` property is an optional boolean can be applied to the containing `RadioGroup`, that reverses the colors so that the radio buttons can better appear on a dark background.

```.jsx
<>
  <RadioGroup
    containerStyle={{ background : '#00263E', color: '#fff', marginTop: '24px', padding: '12px' }}
    labelText='Inverse Radio Buttons'
    id='inverseGroup'
    isInverse
    name='inverse'>
    <Radio
        id='inverseRadio'
        labelText='Inverse'
        value='default' />
      <Radio
          color={magma.colors.success01}
          id='successInverseRadio'
          labelText='Success inverse'
          value='success' />
      <Radio
          color={magma.colors.danger}
          id='dangerInverseRadio'
          labelText='Danger inverse'
          value='danger' />
      <Radio
          color={magma.colors.pop02}
          id='popInverseRadio'
          labelText='Pop inverse'
          value='pop' />
        <Radio
            disabled
            id='disabledInverseRadio'
            labelText='Disabled inverse'
            value='disabled' />
    </RadioGroup>

    <div style={{ background : '#00263E', color: '#fff', marginTop: '24px', padding: '12px' }}>
      <p>* indicates required fields</p>
      <RadioGroup
        errorMessage="Please select a color"
        isInverse
        required
        labelText='Inverse with Error *'
        name='requiredFieldsInverse'>

        <Radio
            labelText='Option 1'
            value='R' />
        <Radio
            labelText='Option2'
            value='B' />
      </RadioGroup>
    </div>
  </>
```

## Custom Styles

Custom styles can be passed into both the RadioGroup component and the Radio component. The `containerStyle` property will apply to the container.
Additional `labelStyle` and `inputStyle` properties are available to style the respective elements. Please use discretion when adding custom styles.

```.jsx
  <RadioGroup
    style={{ 'border': '2px solid yellow', 'padding': '10px'}}
    labelStyle={{ 'color': 'blue', 'textTransform': 'uppercase'}}
    labelText='Custom styles'
    id='customGroup'
    name='custom'>
    <Radio
        containerStyle={{ 'border': '1px dotted purple'}}
        inputStyle={{ 'boxShadow': '3px 3px 3px rgba(0, 0 , 0, .6)'}}
        labelStyle={{ 'color': 'brown', 'fontStyle': 'italic'}}
        labelText='Custom styles'
        value='1' />

    <Radio
        id='custom2'
        labelText='No custom styles'
        value='2' />
  </RadioGroup>
```

## Node as labelText

The `labelText` prop can either be a string, or it can be a React Node.

```.jsx
      () => {

        const labelStyles = {  border: '1px solid #3f3f3f', borderRadius: '3px', display: 'inline-block', padding: '10px' };

        return (
          <RadioGroup labelText='Nodes for labels'>

          <Radio
              labelText={<span style={labelStyles}>Option 1</span>}
              inputStyle={{ alignSelf: 'center' }}
              value="1" />

           <Radio
              labelText={<span style={labelStyles}>Option 2</span>}
              inputStyle={{ alignSelf: 'center' }}
              value="2"  />

          </RadioGroup>
        );
      }
```

## Labeling Radio Groups

To meet accessibility requirements, a radio group _must_ have a label. You can provide a label in one of two ways.

You can use the `labelText` prop, which takes a string. This string will be displayed on screen inside a `<label>` element and be associated with your radio group.

Or you can use the `labelledById` prop, which also takes a string. This string should refer to an ID of an element that exists somewhere else on the page.
This id will become the value of the `aria-labelledby` attribute for the group.

```.jsx
  <>
    <RadioGroup
      labelText="Labelled by Label"
      name='labelledByProp'>
      <Radio
        value='1'
        labelText='Label' />
      <Radio
        value='2'
        labelText='Label' />
    </RadioGroup>


    <RadioGroup
      labelledById="h3Label"
      name='labelledByChildren'>
      <h3 id="h3Label">Labelled by H3</h3>
      <Radio
        value='3'
        labelText='Label' />
      <Radio
        value='4'
        labelText='Label' />
    </RadioGroup>
  </>
```

## Text Position

Use the optional prop `textPosition` to position the label text on either side of the radio button.
The `textPosition` property takes the values `left` and `right`. The default value is `right`.

```.jsx
  <RadioGroup labelText='Text Position' name='textPosition'>
    <Radio
        labelText='Text left'
        textPosition='left'
        value='1' />
    <Radio
        labelText='Text right'
        value='1' />
    </RadioGroup>
```

## Visually Hidden Label

Adding the `isTextVisuallyHidden` prop will make the label text for either the radio group or the radio button available to screen readers, but will hide it visually.

<Alert variant="warning">
  Use caution when using the <code>isTextVisuallyHidden</code> prop; it should
  only be used in rare circumstances when the purpose of the radio button is
  conveyed in another way. While screen readers will announce the label when the
  radio button gains focus, it is still best to have a visual label. This is
  especially helpful for individuals with cognitive disabilities and users who
  have fine motor control limitations.
</Alert>

```.jsx
  <RadioGroup
    labelText='Hidden Label'
    id='visuallyHiddenRadioGroup'
    name='visuallyHiddenRadioGroup'
    isTextVisuallyHidden>
      <Radio
        value='1'
        id='visuallyHiddenRadio1'
        labelText='Hidden label'
        isTextVisuallyHidden />
      <Radio
        value='2'
        id='visuallyHiddenRadio2'
        labelText='Hidden label'
        isTextVisuallyHidden />
    </RadioGroup>
```

## Forward Ref

Using React's `forwardRef` feature you can gain access to the reference of the internal radio.

```.jsx
  (props) => {
    const radioRef = React.useRef();

    return (
      <div>
        <RadioGroup labelText='Forward Ref Usage' id='forwardRefGroup' name='forwardRef'>
          <Radio
            ref={radioRef}
            id='forwardRefRadio1'
            labelText='Option one label'
            value='1' />

          <Radio
            id='forwardRefRadio2'
            labelText='Option two label is really long and can wrap to multiple lines lorem ipsum dolar sit amet is really long and can wrap to multiple lines'
            value='2' />
        </RadioGroup>

        <Button
          onClick={() => {
            radioRef.current.checked = !radioRef.current.checked
          }}>Toggle option 1
        </Button>

          <Button
          color="secondary"
          onClick={() => {
            radioRef.current.focus()
          }}>Focus option 1
        </Button>
      </div>
    )
  }
```

## Testing

Passing in the `testId` prop will add the `data-testid` attribute to the wrapper element for `RadioGroup` and the input element for `Radio` for easier querying in tests.

```html
<div aria-labelledby="basicGroup" data-testid="group-test=id" role="radiogroup">
  <label id="basicGroup">Basic Usage</label>
  <div>
    <input
      id="radio1"
      data-testid="radio-test-id"
      name="basic"
      type="radio"
      value="1"
    />
    <label for="radio1">
      <span> <span color=""></span> </span> Option one label
    </label>
  </div>
</div>
```

## Radio Group Props

**Any other props supplied will be provided to the wrapping `div` element.**

<SimplePropsTable
  propertyValues={{
    children: {
      type: {
        name: 'node',
      },
      required: true,
      description: 'The content of the component',
    },
    containerStyle: {
      type: {
        name: 'React.CSSProperties',
      },
      required: false,
      description: 'Style properties for the component container element',
    },
    errorMessage: {
      type: {
        name: 'ReactNode',
      },
      required: false,
      description:
        'Content of the error message.  If a value is provided, the radio buttons will be styled to show an error state',
      defaultValue: '',
    },
    isInverse: {
      type: {
        name: 'boolean',
      },
      required: false,
      description:
        'If true, the component and all child radio buttons will have inverse styling to better appear on a dark background',
      defaultValue: 'false',
    },
    isTextVisuallyHidden: {
      type: {
        name: 'boolean',
      },
      required: false,
      description:
        'If true, label text for the radio group will be hidden visually, but will still be read by assistive technology',
      defaultValue: 'false',
    },
    labelledById: {
      type: {
        name: 'string',
      },
      required: false,
      description:
        'ID of the element that labels the radio group, used in the aria-labelledby attribute for the group.  Alternatively, the labelText prop can be used.',
      defaultValue: '',
    },
    labelStyle: {
      type: {
        name: 'React.CSSProperties',
      },
      required: false,
      description: 'Style properties for the label of the radio group',
    },
    labelText: {
      type: {
        name: 'string',
      },
      required: false,
      description:
        'Content of label of the radio group; can be node or string. Alternatively, the labelledById prop can be used.',
    },
    onBlur: {
      type: {
        name: 'function',
      },
      required: false,
      description: 'Action that fires when the radio group loses focus',
    },
    onChange: {
      type: {
        name: 'function',
      },
      required: false,
      description:
        'Action that fires when selected value of the radio group changes',
    },
    onFocus: {
      type: {
        name: 'function',
      },
      required: false,
      description:
        'Action that fires when one of the radio buttons within the group receives focus',
    },
    name: {
      type: {
        name: 'string',
      },
      required: false,
      description: 'Name attribute the radio group',
      defaultValue: '',
    },
    required: {
      type: {
        name: 'boolean',
      },
      required: false,
      description:
        'If true, the user must select one radio button for the form to be valid',
      defaultValue: 'false',
    },
    testId: {
      type: {
        name: 'string',
      },
      required: false,
      description:
        'Test ID attached to an internal element as `data-testid` for consumer testing',
    },
    value: {
      type: {
        name: 'string',
      },
      required: false,
      description:
        'Value of the radio button that is the default selected value for the group',
      defaultValue: '',
    },
  }}
/>

## Radio Props

**Any other props supplied will be provided to the `input` element.**

<SimplePropsTable
  propertyValues={{
    color: {
      type: {
        name: 'string',
      },
      required: false,
      description: 'Hex code for the background color',
      defaultValue: '#0000FF',
    },
    containerStyle: {
      type: {
        name: 'React.CSSProperties',
      },
      required: false,
      description: 'Style properties for the component container element',
    },
    disabled: {
      type: {
        name: 'boolean',
      },
      required: false,
      description: 'If true, element is disabled',
      defaultValue: 'false',
    },
    inputStyle: {
      type: {
        name: 'React.CSSProperties',
      },
      required: false,
      description: 'Style properties for the radio button element',
    },
    isInverse: {
      type: {
        name: 'boolean',
      },
      required: false,
      description:
        'If true, the component will have inverse styling to better appear on a dark background',
      defaultValue: 'false',
    },
    isTextVisuallyHidden: {
      type: {
        name: 'boolean',
      },
      required: false,
      description:
        'If true, label text will be hidden visually, but will still be read by assistive technology',
      defaultValue: 'false',
    },
    labelStyle: {
      type: {
        name: 'React.CSSProperties',
      },
      required: false,
      description: 'Style properties for the label element',
    },
    labelText: {
      type: {
        name: 'string',
      },
      required: true,
      description: 'Content of label; can be node or string',
    },
    required: {
      type: {
        name: 'boolean',
      },
      required: false,
      description: 'If true, the form field is required',
      defaultValue: 'false',
    },
    testId: {
      type: {
        name: 'string',
      },
      required: false,
      description:
        'Test ID attached to an internal element as `data-testid` for consumer testing',
    },
    textPosition: {
      type: {
        name: 'enum',
        options: ['RadioTextPosition.left', 'RadioTextPosition.right'],
      },
      required: false,
      description:
        'Whether the label appears to the left of the right of the radio button',
      defaultValue: 'right',
    },
    value: {
      type: {
        name: 'string',
      },
      required: false,
      description: 'Value attribute the form field',
      defaultValue: '',
    },
  }}
/>

export default props => (
  <ScopeableLayout
    {...props}
    components={{ Alert, Radio, RadioGroup, Button, magma }}
  />
);
