---
pageTitle: Button API
title: Button
props:
  - ButtonProps
---

import { ScopeableLayout } from '../../components/layout';
import { Button, LinkButton, Input } from 'react-magma-dom';
import { Link } from 'gatsby';
import DocsHeading from '../../components/docs-heading';
import { ButtonProps } from '../../components/button-props';

<DocsHeading to="/design/button/" type="design">
  Button
</DocsHeading>

## Basic Usage

Buttons are clickable items used to perform an action. See the <Link to="/design/button/">Button Design Guidelines</Link> for more information on when to use the different kinds of buttons.

When used with icons, the Button component takes a slightly different set of props. For information on how to use this component with icons, please see documentation on <Link to="/api/icon-button">Icon Buttons</Link>.

## Sizes

Sizes for buttons include `small`, `medium`, and `large`, with `medium` being the default value.

```.jsx
    <>
        <Button size="small">Small</Button>
        <Button>Default</Button>
        <Button size="large">Large</Button>
    </>
```

## Colors

Colors for buttons include `primary`, `secondary`, `success`, `danger`, and `marketing` with `primary` being the default value.

```.jsx
    <>
        <Button color="primary">Primary (default)</Button>
        <Button color="secondary">Secondary</Button>
        <Button color="success" >Success</Button>
        <Button color="danger">Danger</Button>
        <Button color="marketing">Marketing</Button>
    </>
```

## Variants

Variants for buttons include `solid`, `outline`, and `link`, with `solid` being the default value.

The `marketing` buttons do not have `outline` and `link` variants, and will render as solid buttons.

```.jsx
    <>
      <p>
          <Button color="primary" variant="solid">Solid (default)</Button>
          <Button color="primary" variant="outline">Outline</Button>
          <Button color="primary" variant="link">Link</Button>
      </p>
      <p>
          <Button color="secondary" variant="solid">Solid (default)</Button>
          <Button color="secondary" variant="outline">Outline</Button>
          <Button color="secondary" variant="link">Link</Button>
      </p>
      <p>
          <Button color="success" variant="solid">Solid (default)</Button>
          <Button color="success" variant="outline">Outline</Button>
          <Button color="success" variant="link">Link</Button>
      </p>
      <p>
          <Button color="danger" variant="solid">Solid (default)</Button>
          <Button color="danger" variant="outline">Outline</Button>
          <Button color="danger" variant="link">Link</Button>
      </p>
    </>
```

## Link Buttons

Using the `LinkButton` will render a button that is styled exactly like a link. Unlike using the `<Button variant='link' />`,
the `LinkButton` will render as inline text, the way an `a` element would.

**Note:** For accessibility, it's important to remember that this is still a button and should be used to trigger functionality or manipulate data, and _not_ navigate to a new page like a traditional hyperlink.

```.jsx
    <>
        This is a <LinkButton>link button</LinkButton>.
    </>
```

## Shapes

Shapes for buttons include `fill`, `leftCap`, and `rightCap`, with `fill` being the default value.

```.jsx
    <>
        <Button color="primary">Fill (default)</Button>
        <Button color="primary" shape="leftCap">Left Cap</Button>
        <Button color="primary" shape="rightCap">Right Cap</Button>
    </>
```

## Inverse

The `isInverse` property is an optional boolean, that reverses the colors so that the buttons can better appear on a dark background.
The default value is `false`.

While `outline isInverse buttons` and `link isInverse buttons` still accept the `color` property, the `color` property
online affects `solid isInverse buttons`.

The `marketing` button does not have an inverse state, as it already is styled to appear well against a dark background.

```.jsx
    <div style={{ background : '#00263E', color: '#ffffff', padding: '10px' }}>
      <p>
          <Button isInverse>Solid inverse</Button>
          <Button color="secondary" isInverse>Solid inverse</Button>
          <Button color="success" isInverse>Solid inverse</Button>
          <Button color="danger" isInverse>Solid inverse</Button>
          <Button color="marketing" isInverse>Marketing inverse</Button>
      </p>
      <p>
          <Button variant="outline" isInverse>Outline inverse</Button>
          <Button variant="link" isInverse>Link inverse</Button>
      </p>
      <p>
          <Button disabled isInverse>Disabled inverse</Button>
          <Button disabled isInverse variant="outline">Disabled outline inverse</Button>
          <Button disabled isInverse variant="link">Disabled link inverse</Button>
      </p>
      <p>
        This is a <LinkButton isInverse>link button</LinkButton>.
      </p>
      <p>
        This is a <LinkButton disabled isInverse>disabled link button</LinkButton>.
      </p>
    </div>
```

## Text Transform

Options for the `textTransform` prop for buttons include `uppercase` and `none`, with `uppercase` (all caps) being the default value.
This sets the CSS `text-transform` property.

```.jsx
    <>
        <Button>All caps</Button>
        <Button textTransform="none">No Text Transform</Button>
        <Button textTransform="uppercase">Uppercase transform</Button>
    </>
```

## Full Width

Passing the `isFullWidth` property will set the button to display full-width.

```.jsx
    <>
        <Button isFullWidth>Full Width button</Button>
        <Button isFullWidth color="secondary">Full Width button</Button>
    </>
```

## Disabled

```.jsx
    <>
        <Button disabled>Disabled</Button>
        <Button disabled variant="outline">Disabled</Button>
        <Button disabled variant="link">Disabled</Button>
        <p>This is a <LinkButton disabled>disabled link button</LinkButton>.</p>
    </>
```

## Types

The button types `submit` and `reset` are able to be passed in to change the buttons behavior in forms.
By default, buttons will have a type of `button`.

```.jsx
  (props) => {
    const inputRef = React.useRef();

    function onFormSubmit(event) {
      event.preventDefault();
      alert('Form Submitted')
    }

    function onFormReset(event) {
      event.preventDefault();
      inputRef.current.value = '';
    }

    return (<form onSubmit={onFormSubmit} onReset={onFormReset}>
      <Input
        ref={inputRef}
        labelText='Form Field'
        value='' />
      <Button type="submit">Submit</Button>
      <Button type="reset">Reset</Button>
    </form>)
  }
```

## Event Handling

The `onClick` event will fire when the button is clicked, or when the enter or space bar key is pressed.
Events do not fire on disabled buttons.

```.jsx
  (props) => {
    const [counter, setCounter] = React.useState(0);

    return <div>
      <p><strong>Counter: </strong> <span id="counter">{counter}</span></p>
      <Button
        onClick={() => {
          setCounter(count => count + 1)
        }}>Click me</Button>

      <Button
        disabled
        onClick={() => {
          setCounter(count => count + 1)
        }}>Click me</Button>
    </div>
  }
```

## Forward Ref

Using React's `forwardRef` feature you can gain access to the reference of the internal button.

```.jsx
  (props) => {
    const buttonRef = React.useRef();

    return <div>
      <Button
        ref={buttonRef}>Button to be focused</Button>

      <Button
        onClick={() => {
          buttonRef.current.focus();
        }}>Click to focus other button</Button>
    </div>
  }
```

## Testing

Passing in the `testId` prop will add the `data-testid` attribute to the button element for easier querying in tests.

```html
<button data-testid="test-id">Default</button>
```

## Button Props

**This component uses `forwardRef`. The ref is applied to the `button` element.**

All of the [standard button attributes](https://developer.mozilla.org/en-US/docs/Web/HTML/Element/button) can be provided as props and will be applied to the `button` element that gets rendered.

<ButtonProps />

export default props => (
  <ScopeableLayout {...props} components={{ Button, LinkButton, Input }} />
);
