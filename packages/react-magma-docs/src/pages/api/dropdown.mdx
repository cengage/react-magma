---
title: 'Dropdown'
---

import { ScopeableLayout } from '../../components/layout'
import {
  AsteriskIcon,
  BellIcon,
  MenuIcon,
  Checkbox,
  Dropdown,
  DropdownMenu,
  DropdownMenuDivider,
  DropdownMenuHeader,
  DropdownMenuItem,
  DropdownToggle,
} from 'react-magma-dom'
import { SimplePropsTable } from '../../components/props-table'
import { Link } from 'gatsby'

# Dropdown

## Basic Usage

```.jsx
      <Dropdown>
         <DropdownToggle>Basic Dropdown</DropdownToggle>
          <DropdownMenu>
            <DropdownMenuItem>Menu item 1</DropdownMenuItem>
            <DropdownMenuItem>Menu item number two</DropdownMenuItem>
          </DropdownMenu>
      </Dropdown>
```

## Drop Direction

```.jsx
      <Dropdown dropDirection='up'>
          <DropdownToggle>Drop up</DropdownToggle>
          <DropdownMenu>
            <DropdownMenuItem>Menu item 1</DropdownMenuItem>
            <DropdownMenuItem>Menu item number two</DropdownMenuItem>
          </DropdownMenu>
      </Dropdown>
```

## Alignment

```.jsx
      <Dropdown alignment='right'>
        <DropdownToggle>Right Aligned</DropdownToggle>
        <DropdownMenu>
          <DropdownMenuItem>Menu item 1</DropdownMenuItem>
          <DropdownMenuItem>Menu item number two</DropdownMenuItem>
        </DropdownMenu>
      </Dropdown>
```

## Fixed Width

By default, a dropdown menu will be as wide as the largest item. However, you may limit the width using the `width` prop, in which case items will wrap if needed.

```.jsx
      <Dropdown width="250px">
        <DropdownToggle>Fixed Width</DropdownToggle>
        <DropdownMenu>
          <DropdownMenuItem>Lorem ipsum dolor sit amet, consectetur adipiscing elit. Nulla tellus turpis, finibus et bibendum eget, rhoncus ut risus. Proin nec nisi ut eros aliquam interdum.</DropdownMenuItem>
          <DropdownMenuItem>Nunc egestas ullamcorper finibus. Phasellus non risus id mauris dignissim molestie in sit amet enim. In condimentum fringilla sodales.</DropdownMenuItem>
        </DropdownMenu>
      </Dropdown>
```

## Disabled Item

```.jsx
      <Dropdown>
        <DropdownToggle>Disabled Item</DropdownToggle>
        <DropdownMenu>
          <DropdownMenuItem isDisabled onClick={()=>{alert('Item 1 clicked')}}>Menu item 1</DropdownMenuItem>
          <DropdownMenuItem  onClick={()=>{alert('Item 2 clicked')}}>Menu item 2</DropdownMenuItem>
        </DropdownMenu>
      </Dropdown>
```

## Extra Toggle Button Props

```.jsx
      <>
        <Dropdown>
          <DropdownToggle size="small" color="success" variant='outline'>Extra Props Example 1</DropdownToggle>
            <DropdownMenu>
              <DropdownMenuItem>Menu item 1</DropdownMenuItem>
              <DropdownMenuItem>Menu item number two</DropdownMenuItem>
            </DropdownMenu>
        </Dropdown>
        <Dropdown>
          <DropdownToggle aria-label="Extra props example 2" size='large' variant='link' icon={<MenuIcon />} />
            <DropdownMenu>
              <DropdownMenuItem>Menu item 1</DropdownMenuItem>
              <DropdownMenuItem>Menu item number two</DropdownMenuItem>
            </DropdownMenu>
        </Dropdown>
      </>
```

## Leading Icons

```.jsx
      <Dropdown>
        <DropdownToggle>Leading Icons</DropdownToggle>
        <DropdownMenu>
          <DropdownMenuItem icon={<AsteriskIcon />}>Menu item 1</DropdownMenuItem>
          <DropdownMenuItem icon={<BellIcon />}>Menu item 2</DropdownMenuItem>
        </DropdownMenu>
      </Dropdown>
```

## Divider

```.jsx
      <Dropdown>
        <DropdownToggle>Divider</DropdownToggle>
        <DropdownMenu>
          <DropdownMenuItem>Menu item 1</DropdownMenuItem>
          <DropdownMenuItem>Menu item 2</DropdownMenuItem>
          <DropdownMenuDivider />
          <DropdownMenuItem>Log out</DropdownMenuItem>
        </DropdownMenu>
      </Dropdown>
```

## Divider with Headings

```.jsx
      <Dropdown>
        <DropdownToggle>Divider and headings</DropdownToggle>
        <DropdownMenu>
          <DropdownMenuHeader>Section title A</DropdownMenuHeader>

          <DropdownMenuItem>Menu item 1</DropdownMenuItem>
          <DropdownMenuItem>Menu item 2</DropdownMenuItem>
          <DropdownMenuDivider />
          <DropdownMenuHeader>Section title B</DropdownMenuHeader>
          <DropdownMenuItem>Menu item 3</DropdownMenuItem>
          <DropdownMenuItem>Menu item 4</DropdownMenuItem>
        </DropdownMenu>
      </Dropdown>
```

## Dropdown Props

**Any other props supplied will be provided to the wrapping `div` element.**

<SimplePropsTable
  propertyValues={{
    alignment: {
      type: {
        name: 'enum',
        options: ['DropdownAlignment.left, DropdownAlignment.right'],
      },
      required: false,
      description: 'Alignment of the dropdown menu',
      defaultValue: 'left',
    },
    dropDirection: {
      type: {
        name: 'enum',
        options: ['DropdownDropDirection.down, DropdownDropDirection.up'],
      },
      required: false,
      description: 'Position of the dropdown menu',
      defaultValue: 'down',
    },
    testId: {
      type: {
        name: 'string',
      },
      required: false,
      description:
        'Test ID attached to an internal element as `data-testid` for consumer testing',
    },
    width: {
      type: {
        name: 'string',
      },
      required: false,
      description: 'Width of menu',
      defaultValue: 'Width of longest menu item',
    },
  }}
/>

## Dropdown Toggle Props

**Dropwdown Toggle accepts the same props as the <Link to="/api/icon-button">Icon Button</Link> component.**

## Dropdown Menu Props

**Props supplied will be provided to the wrapping `div` element.**

## Dropdown Menu Item

**Any other props supplied will be provided to the wrapping `li` element.**

<SimplePropsTable
  propertyValues={{
    icon: {
      type: {
        name: 'React.ReactElement',
      },
      required: false,
      description: 'Leading icon for the menu item',
    },
    isDisabled: {
      type: {
        name: 'boolean',
      },
      required: false,
      description:
        'If true, item will appear dimmed and onClick event will not fire',
      defaultValue: 'false',
    },
    onClick: {
      type: {
        name: 'function',
      },
      required: false,
      description: 'Action that fires when the menu item is clicked.',
    },
  }}
/>

export default props => (
  <ScopeableLayout
    {...props}
    components={{
      AsteriskIcon,
      BellIcon,
      MenuIcon,
      Checkbox,
      Dropdown,
      DropdownMenu,
      DropdownMenuDivider,
      DropdownMenuHeader,
      DropdownMenuItem,
      DropdownToggle,
    }}
  />
)
