---
title: 'Tabs'
---

import { ScopeableLayout } from '../../components/layout'
import {
  magma,
  Tab,
  Tabs,
  TabPanel,
  TabsContainer,
  GraduationCapIcon,
  BellIcon,
  AndroidIcon,
  HyperLink,
} from 'react-magma-dom'
import { SimplePropsTable } from '../../components/props-table'
import { Link } from 'gatsby'

# Tabs

## Basic Usage

The tab system is made up of four components:
`TabContainer`, which contains both the tabs navigation and the respective content. `Tabs`, which contains the tabs navigation bar.
`Tab`, which is each individual menu item within the tabs navigation bar. And `TabPanel`, which contains the indivdual content sections for each tab.

```.jsx
      function TabComponent() {

        const wrapperStyles = {
          backgroundColor: '#F7F7F7',
          padding: '25px'
        }

        const panelStyles = {
          height: '100px',
          padding: '5px',
          backgroundColor: '#fff'
        }

        return (
          <TabsContainer style={wrapperStyles}>
              <Tabs>
                <Tab>Main page with a longer text label</Tab>
                <Tab>FAQ</Tab>
                <Tab>About us</Tab>
              </Tabs>

              <TabPanel index={0}>
                <div style={panelStyles}>Main page</div>
              </TabPanel>
              <TabPanel index={1}>
                <div style={panelStyles}>FAQ</div>
              </TabPanel>
              <TabPanel index={2}>
                <div style={panelStyles}>About us</div>
              </TabPanel>
          </TabsContainer>
    )}
```

# Border Position

By default, horizontal tabs have the inidicator placed underneath the tabs.
However, there are times you may want to place the indicator above the tabs, such as when the tab menu is displayed below its content.
The `borderPosition` prop is an enum and accepts `top` or `bottom` for horizontal tabs. The border position for vertical tabs is always `left`.

The `borderPosition` prop is specified on the `Tabs` component and will apply to each individual tab.

```.jsx
      function TabComponent() {
        const wrapperStyles = {
          backgroundColor: '#F7F7F7',
          padding: '25px'
        }

        const panelStyles = {
          height: '100px',
          padding: '5px',
          backgroundColor: '#fff'
        }

        return (
          <div style={wrapperStyles}>
            <TabsContainer>
              <TabPanel index={0}>
                  <div style={panelStyles}>Main page</div>
                </TabPanel>
                <TabPanel index={1}>
                  <div style={panelStyles}>FAQ</div>
                </TabPanel>
                <TabPanel index={2}>
                  <div style={panelStyles}>About us</div>
                </TabPanel>
                <Tabs borderPosition='top'>
                  <Tab>Main page</Tab>
                  <Tab>FAQ</Tab>
                  <Tab>About us</Tab>
                </Tabs>
            </TabsContainer>
          </div>
    )}
```

## Custom Logic

This is a simple example of Tab Components with custom switching logic

```.jsx
      class TabComponent extends React.Component {
        constructor(props) {
          super(props);
          this.state = {
            activeIndex: 0,
          }

          this.wrapperStyles = {
            backgroundColor: '#F7F7F7',
            padding: '25px'
          }

          this.panelStyles = {
            height: '100px',
            padding: '5px',
            backgroundColor: '#fff'
          }

          this.handleChange = this.handleChange.bind(this)
        }

        handleChange(index) {
          this.setState(() => ({
            activeIndex: index,
          }))
        }

        render() {
          return (
              <div style={this.wrapperStyles}>
                <TabsContainer>
                    <Tabs
                      activeIndex={this.state.activeIndex}
                      onChange={this.handleChange}
                    >
                      <Tab index={0}>First item</Tab>
                      <Tab index={1}>Second item</Tab>
                      <Tab index={2}>Third item</Tab>
                    </Tabs>

                    <TabPanel index={0} activeIndex={this.state.activeIndex}>
                      <div style={this.panelStyles}>First item</div>
                    </TabPanel>
                    <TabPanel index={1} activeIndex={this.state.activeIndex}>
                      <div style={this.panelStyles}>Second item</div>
                    </TabPanel>
                    <TabPanel index={2} activeIndex={this.state.activeIndex}>
                      <div style={this.panelStyles}>Third item</div>
                    </TabPanel>
                </TabsContainer>
              </div>
          )
        }
      }

```

## Disabled Tabs

If a `Tab` is `disabled`, it will not be actionable. It will still appear in the menu, but will be styled to appear greyed out.

```.jsx
      function TabComponent() {
        const wrapperStyles = {
          backgroundColor: '#F7F7F7',
          padding: '25px'
        }

        const panelStyles = {
          height: '100px',
          padding: '5px',
          backgroundColor: '#fff'
        }

        return (
          <div style={wrapperStyles}>
            <TabsContainer>
                <Tabs>
                  <Tab>Main page</Tab>
                  <Tab disabled>FAQ</Tab>
                  <Tab>About us</Tab>
                </Tabs>

                <TabPanel index={0}>
                  <div style={panelStyles}>Main page</div>
                </TabPanel>
                <TabPanel index={1}>
                  <div style={panelStyles}>FAQ</div>
                </TabPanel>
                <TabPanel index={2}>
                  <div style={panelStyles}>About us</div>
                </TabPanel>
            </TabsContainer>
          </div>
    )}
```

## Centered Tabs

The `isCentered` prop is an optional boolean that will style the tabs in the center of their container.

```.jsx
      function TabComponent() {
        return (
          <TabsContainer>
              <Tabs isCentered>
                <Tab>Main page</Tab>
                <Tab>FAQ</Tab>
                <Tab>About us</Tab>
              </Tabs>
          </TabsContainer>
    )}
```

## Full-Width Tabs

The `isFullWidth` prop is an optional boolean that will cause the tabs to take up the full width of their container. The `isFullWidth` prop is specified on the `Tabs` component.

```.jsx
      function TabComponent() {
        return (
          <TabsContainer>
            <Tabs isFullWidth>
              <Tab>Main Page with a longer text label</Tab>
              <Tab>FAQ</Tab>
              <Tab>About Us</Tab>
            </Tabs>
        </TabsContainer>
    )}
```

## Scrollable Tabs with Buttons

The `hasScrollButtons` prop can be used to display navigation buttons for when the tabs do not fit within the visual display area.

```.jsx
      function TabComponent() {
        const wrapperStyles = {
          backgroundColor: '#F7F7F7',
          padding: '25px'
        }

        const panelStyles = {
          height: '100px',
          padding: '5px',
          backgroundColor: '#fff'
        }

        return (
          <div style={wrapperStyles}>
            <TabsContainer>
                <Tabs hasScrollButtons>
                  <Tab>First item with a longer text label</Tab>
                  <Tab>Second item</Tab>
                  <Tab>Third item</Tab>
                  <Tab>Fourth item</Tab>
                  <Tab>Fifth item</Tab>
                  <Tab>Sixth item</Tab>
                </Tabs>

                <TabPanel index={0}>
                  <div style={panelStyles}>First item</div>
                </TabPanel>
                <TabPanel index={1}>
                  <div style={panelStyles}>Second item</div>
                </TabPanel>
                <TabPanel index={2}>
                  <div style={panelStyles}>Third item</div>
                </TabPanel>
                <TabPanel index={3}>
                  <div style={panelStyles}>Fourth item</div>
                </TabPanel>
                <TabPanel index={4}>
                  <div style={panelStyles}>Fifth item</div>
                </TabPanel>
                <TabPanel index={5}>
                  <div style={panelStyles}>Sixth item</div>
                </TabPanel>
            </TabsContainer>
          </div>
    )}
```

## Scrollable Tabs Without Buttons

This is an example of scrollable Tabs without buttons

```.jsx
      function TabComponent() {
        const wrapperStyles = {
          backgroundColor: '#F7F7F7',
          padding: '25px'
        }

        const panelStyles = {
          height: '100px',
          padding: '5px',
          backgroundColor: '#fff'
        }

        return (
          <div style={wrapperStyles}>
            <TabsContainer>
                <Tabs>
                  <Tab>First item</Tab>
                  <Tab>Second item</Tab>
                  <Tab>Third item</Tab>
                  <Tab>Fourth item</Tab>
                  <Tab>Fifth item</Tab>
                  <Tab>Sixth item</Tab>
                </Tabs>

                <TabPanel index={0}>
                  <div style={panelStyles}>First item</div>
                </TabPanel>
                <TabPanel index={1}>
                  <div style={panelStyles}>Second item</div>
                </TabPanel>
                <TabPanel index={2}>
                  <div style={panelStyles}>Third item</div>
                </TabPanel>
                <TabPanel index={3}>
                  <div style={panelStyles}>Fourth item</div>
                </TabPanel>
                <TabPanel index={4}>
                  <div style={panelStyles}>Fifth item</div>
                </TabPanel>
                <TabPanel index={5}>
                  <div style={panelStyles}>Sixth item</div>
                </TabPanel>
            </TabsContainer>
          </div>
    )}
```

## Vertical Tabs

The `orientation` prop can be used to display tabs vertically, instead of horizontalally.  
The `orientation` prop is an enum that accepts either `horizontal` or `vertical` with `horizontal` being the default.
This prop is specified on the `Tabs` component.

```.jsx
      function TabComponent() {
        const wrapperStyles = {
          backgroundColor: '#F7F7F7',
          padding: '25px',
        }

        const panelStyles = {
          height: '100%',
          padding: '5px',
          backgroundColor: '#fff'
        }

        return (
          <div style={wrapperStyles}>
            <TabsContainer>
                <Tabs orientation='vertical'>
                  <Tab>Main page with a longer text label</Tab>
                  <Tab>FAQ</Tab>
                  <Tab>About us</Tab>
                </Tabs>

                <TabPanel index={0}>
                  <div style={panelStyles}>Main page</div>
                </TabPanel>
                <TabPanel index={1}>
                  <div style={panelStyles}>FAQ</div>
                </TabPanel>
                <TabPanel index={2}>
                  <div style={panelStyles}>About us</div>
                </TabPanel>
            </TabsContainer>
          </div>
    )}
```

## Nav Tabs

This is an example with custom element in Tabs.
By default we use `button` element in Tabs, but you can include your custom element inside a `Tab` component.
In this case, please put all children inside the `component` prop instead of putting the content directly inside each `Tab`.
If you would like to use `Tab` like an route switcher, keep the `path` prop in `Tab` equal to desired path.

```.jsx
    <Tabs orientation='horizontal'>
      <Tab path="/main" component={<Link to="/api/tabs/#nav_tabs">Main page</Link>}/>
      <Tab path="/faq" component={<Link to="/api/tabs/#nav_tabs">FAQ</Link>}/>
      <Tab path="/about" component={<Link to="/api/tabs/#nav_tabs">About us</Link>}/>
    </Tabs>
```

## Icon Tabs

The `icon` prop can be used to display an icon inside the individual `Tab` components.

### Icon Only Tabs

`Tab` components without children can be used to display an icon only version of the tabs. Be sure to include an `ariaLabel` for each tab, when using this pattern.

```.jsx
      function TabComponent() {
        return (
          <TabsContainer>
              <Tabs>
                <Tab ariaLabel='Graduation' icon={<GraduationCapIcon />} />
                <Tab ariaLabel='Android' icon={<AndroidIcon />} />
                <Tab ariaLabel='Bell' icon={<BellIcon />} />
              </Tabs>
          </TabsContainer>
    )}
```

### Icon With Text

`Tab` components with children and and `icon` prop will display both the icon and the children.
By default, the icons appear above the children.

```.jsx
      function TabComponent() {
        return (
          <TabsContainer>
              <Tabs>
                <Tab icon={<GraduationCapIcon />}>First item with a longer text label</Tab>
                <Tab icon={<AndroidIcon />}>Second item</Tab>
                <Tab icon={<BellIcon />}>Third item</Tab>
              </Tabs>
          </TabsContainer>
    )}
```

### Icon With Text, Icon Left-Aligned

The `iconOrientation` prop can be used to position the icon to the left of the children.  
The `iconOrientation` prop is an enum that accepts either `left` or `top` with `top` being the default for horizontal tabs.
This prop is specified on the `Tabs` component.

```.jsx
      function TabComponent() {
        return (
          <TabsContainer>
              <Tabs iconOrientation="left">
                <Tab icon={<GraduationCapIcon />}>First item with a longer text label</Tab>
                <Tab icon={<AndroidIcon />}>Second item</Tab>
                <Tab icon={<BellIcon />}>Third item</Tab>
              </Tabs>
          </TabsContainer>
    )}
```

## Vertical Tabs with Icons

Vertical tabs with icons always display with the icon to the left of the text.

```.jsx
      function TabComponent() {
        const wrapperStyles = {
          backgroundColor: '#F7F7F7',
          padding: '25px',
        }

        const panelStyles = {
          height: '100%',
          padding: '5px',
          backgroundColor: '#fff'
        }

        return (
          <div style={wrapperStyles}>
            <TabsContainer>
                <Tabs iconOrientation="left" orientation='vertical'>
                  <Tab icon={<GraduationCapIcon />}>First item with a longer text label</Tab>
                  <Tab icon={<AndroidIcon />}>Second item</Tab>
                  <Tab icon={<BellIcon />}>Third item</Tab>
                </Tabs>
                <TabPanel index={0}>
                  <div style={panelStyles}>Main page</div>
                </TabPanel>
                <TabPanel index={1}>
                  <div style={panelStyles}>FAQ</div>
                </TabPanel>
                <TabPanel index={2}>
                  <div style={panelStyles}>About us</div>
                </TabPanel>
            </TabsContainer>
          </div>
    )}
```

## Inverse

The `isInverse` property is an optional boolean, that reverses the colors so that the tabs can better appear on a dark background.
The default value is false.

### With Blue Background

```.jsx
      function TabComponent() {
        const wrapperStyles = {
          backgroundColor: '#0f3765'
        }

        return (
          <div style={wrapperStyles}>
            <TabsContainer>
                <Tabs isInverse={true}>
                  <Tab>First item</Tab>
                  <Tab>Second item</Tab>
                  <Tab>Third item</Tab>
                </Tabs>
            </TabsContainer>
          </div>
    )}
```

### With Dark Background

```.jsx
      function TabComponent() {
        const wrapperStyles = {
          backgroundColor: '#3f3f3f'
        }

        return (
          <div id="example wrapper" style={wrapperStyles}>
            <TabsContainer>
                <Tabs isInverse={true}>
                  <Tab>First item</Tab>
                  <Tab>Second item</Tab>
                  <Tab>Third item</Tab>
                </Tabs>
            </TabsContainer>
          </div>
    )}
```

## Tabs Container API

<SimplePropsTable
  props={{
    testId: {
      type: {
        name: 'string',
      },
      required: false,
      description: 'The id for react-testing-library.',
    },
  }}
/>

## Tabs API

<SimplePropsTable
  props={{
    activeId: {
      type: {
        name: 'number',
      },
      required: false,
      description:
        'The id of current active tab. You can use this for managing state of tabs component by your custom logic.',
    },
    ariaLabel: {
      type: {
        name: 'string',
      },
      required: false,
      description: 'Custom aria label for Tabs component.',
    },
    borderPosition: {
      type: {
        name: 'enum',
        options: [
          'TabsBorderPositionHorizontal.bottom',
          'TabsBorderPositionHorizontal.top',
          'TabsBorderPositionVertical.left',
        ],
      },
      required: false,
      description: 'Position of the current tab indicator border.',
      defaultValue: 'bottom or left',
    },
    iconOrientation: {
      type: {
        name: '"left" | "top"',
      },
      required: false,
      description: 'The orientation of icon on Tab.',
    },
    isCentered: {
      type: {
        name: 'boolean',
      },
      required: false,
      description: 'The prop for turning on/off centered mode.',
    },
    isInverse: {
      type: {
        name: 'boolean',
      },
      required: false,
      description:
        'If true, the component will have inverse styling to better appear on a dark background.',
      defaultValue: 'false',
    },
    hasScrollButtons: {
      type: {
        name: 'boolean',
      },
      required: false,
      description:
        'The prop for turning on/off buttons for scrolling Tabs component.',
    },
    onChange: {
      type: {
        name: 'function',
      },
      required: false,
      description:
        'The onChange handler for managing state of Tabs component by your custom logic.',
    },
    testId: {
      type: {
        name: 'string',
      },
      required: false,
      description: 'The id for react-testing-library.',
    },
  }}
/>

## Tab API

<SimplePropsTable
  props={{
    ariaLabel: {
      type: {
        name: 'string',
      },
      required: false,
      description: 'Custom aria label for Tab component.',
    },
    component: {
      type: {
        name: 'React.ReactElement<any>',
      },
      required: false,
      description: 'The prop for custom component instead of <button> in Tab.',
    },
    disabled: {
      type: {
        name: 'boolean',
      },
      required: false,
      description: 'The prop for disabling Tab.',
    },
    index: {
      type: {
        name: 'number',
      },
      required: false,
      description: 'The index of your Tab.',
    },
    isActive: {
      type: {
        name: 'boolean',
      },
      required: false,
      description: 'The activity state of Tab component for your custom logic.',
    },
    path: {
      type: {
        name: 'string',
      },
      required: false,
      description: `The path link. It's needed in case you are using Link component for example by React Router library.
        Path must be equal to expected route.`,
    },
    testId: {
      type: {
        name: 'string',
      },
      required: false,
      description: 'The id for react-testing-library.',
    },
  }}
/>

## TabPanel API

<SimplePropsTable
  props={{
    activeIndex: {
      type: {
        name: 'number',
      },
      required: false,
      description:
        'The index of current active tab. You can use this for managing state of tabs component by your custom logic.',
    },
    index: {
      type: {
        name: 'number',
      },
      required: false,
      description: 'The index of your Tab.',
    },
    testId: {
      type: {
        name: 'string',
      },
      required: false,
      description: 'The id for react-testing-library',
    },
  }}
/>

export default props => (
  <ScopeableLayout
    {...props}
    components={{
      Tab,
      Tabs,
      TabPanel,
      TabsContainer,
      magma,
      GraduationCapIcon,
      BellIcon,
      AndroidIcon,
      HyperLink,
      Link,
    }}
  />
)
