---
pageTitle: Form API
title: Form
props:
  - FormProps
---

import { ScopeableLayout } from '../../components/layout';
import { Form, Button, ButtonType, Input } from 'react-magma-dom';
import { DocsHeading } from '../../components/DocsHeading';

<DocsHeading to="/design/form/" type="api">
  Form
</DocsHeading>

The `Form` component is a relatively simple container. It is used to contain interactive controls and sublit information to a web server.

## Basic Usage

```.jsx
<Form
  onSubmit={() => alert('form submitted')}
  header="Form Heading"
  description="Some Form Description"
  errorMessage="Some Form Error"
  actions={
    <div>
      <Button type={ButtonType.submit}>Submit</Button>
    </div>
  }
>
    <Input labelText="some input" />
</Form>
```

# Form Props

**Any other props supplied will be provided to the wrapping `form` element.**

<FormProps />

export default props => (
  <ScopeableLayout
    {...props}
    components={{ Form, Button, ButtonType, Input }}
  />
);
