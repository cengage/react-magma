---
name: Button
menu: Components
---

# Buttons

### Buttons are clickable items used to perform an action.

Buttons should be used in situations where users might need to:

* Submit a form.
* Begin a new task.
* Trigger a new UI element to appear on the page.
* Specify a new or next step in a process.



import { Playground, PropsTable } from 'docz'
import { Button } from 'react-magma-dom';

### Sizes

<Playground>
    <p>
        <Button type="primary" size="small" text="Small Button" />
        <Button type="primary" text="Default Button" />
        <Button type="primary" size="large" text="Large Button" />
    </p>    
</Playground>

### Types

<Playground>
    <p>
        <Button text="Default Button" />
        <Button type="primary" text="Primary Button" />
        <Button type="success" text="Success Button" />
        <Button type="warning" text="Warning Button" />
        <Button type="danger" text="Danger Button" />
    </p>
</Playground>

### Ghost

<Playground>
    <p>
        <Button ghost text="Default Ghost Button" />{' '}
        <Button ghost type="primary" text="Primary Ghost Button" />
        <Button ghost type="success" text="Success Ghost Button" />
        <Button ghost type="warning" text="Warning Ghost Button" />
        <Button ghost type="danger" text="Danger Ghost Button" />
    </p>
</Playground>

### Disabled

<Playground>
    <p>
        <Button disabled type="primary" text="Disabled Button" />
    </p>
</Playground>

### Link

<Playground>
    <p>
        <Button type="link" text="Link Button"/>
    </p>
</Playground>

### Props
