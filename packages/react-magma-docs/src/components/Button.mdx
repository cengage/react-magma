---
name: Button
menu: Components
---

# Buttons

### Buttons are clickable items used to perform an action.

Buttons should be used in situations where users might need to:

* Submit a form.
* Begin a new task.
* Trigger a new UI element to appear on the page.
* Specify a new or next step in a process.



import { Playground, PropsTable } from 'docz'
import { Button } from 'react-magma-dom';

### Sizes

<Playground>
    <p>
        <Button type="primary" size="small" text="Small" />
        <Button type="primary" text="Default" />
        <Button type="primary" size="large" text="Large" />
    </p>    
</Playground>

### Types

<Playground>
    <p>
        <Button text="Default" />
        <Button type="primary" text="Primary" />
        <Button type="success" text="Success" />
        <Button type="warning" text="Warning" />
        <Button type="danger" text="Danger" />
    </p>
</Playground>

### Ghost

<Playground>
    <p>
        <Button ghost text="Default Ghost" />{' '}
        <Button ghost type="primary" text="Primary Ghost" />
        <Button ghost type="success" text="Success Ghost" />
        <Button ghost type="warning" text="Warning Ghost" />
        <Button ghost type="danger" text="Danger Ghost" />
    </p>
</Playground>

### Disabled

<Playground>
    <p>
        <Button disabled type="primary" text="Disabled" />
    </p>
</Playground>

### Link

<Playground>
    <p>
        <Button type="link" text="Link"/>
    </p>
</Playground>

### Click Events

<Playground>
  {() => {
    let counter = 0;
    return <div>
      <p><strong>Counter: </strong> <span id="counter">{counter}</span></p>
      <Button
        handleClick={() => {
          counter += 1;
          document.getElementById('counter').textContent = counter;
        }}
        text="Click me" />
    </div>
  }}
</Playground>

### Props
