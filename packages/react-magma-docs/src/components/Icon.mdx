---
name: Icon
menu: Components
---

import { Playground } from 'docz';
import { IconContainer } from '../docs-components/IconContainer';
import { IconListContainer } from '../docs-components/IconListContainer';
import { Icon, ICONS } from 'react-magma-dom';

# Icons

## Basic Usage

<Playground>
  <p>
    <IconContainer>
      <Icon id="infoBasic" title="Info Available" type="info" />
    </IconContainer>
  </p>
</Playground>

## Sizes

<Playground>
  <p>
    <IconContainer>
      <Icon id="smallSize" size={20} title="Small Size" type="angle-down" />
    </IconContainer>
    <IconContainer>
      <Icon id="largeSize" size={50} title="Large Size" type="angle-down" />
    </IconContainer>
  </p>
</Playground>

## Color

<Playground>
  <p>
    <IconContainer>
      <Icon id="noColor" title="No Color" type="angle-down" />
    </IconContainer>
    <IconContainer>
      <Icon id="someColor" color="green" title="Some Color" type="angle-down" />
    </IconContainer>
  </p>
</Playground>

## Available Icons

<Playground>
  {
    <IconListContainer>{Object.keys(ICONS).map((key) => {
      const id = key + 'Available';
      const title = key + ' Title';

      return <IconContainer border>
        <Icon id={id} title={title} type={key} /><br />
        {key}
      </IconContainer>
    })}</IconListContainer>
  }
</Playground>
