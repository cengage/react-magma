---
'@react-magma/charts': patch
'react-magma-dom': patch
---

fix(Charts): Add optional `ariaLabel` property for SVG container. The default "Interactive chart" label is no longer applied when `ariaLabel` is not provided.
