---
'react-magma-dom': patch
---

fix(NativeSelect): Add `aria-invalid` for error state, update `aria-describedby` reference, add `aria-labelledby` for additional content, and skip disabled options during keyboard navigation.
