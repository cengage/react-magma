---
'react-magma-dom': minor
---

feat(theme): emit `--magma-*` CSS custom properties from `GlobalStyles` and add `tk()` helper for token-based theming. `Alert`/`AlertBase` migrated as the first example
