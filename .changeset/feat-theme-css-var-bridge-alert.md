---
'react-magma-dom': minor
'react-magma-docs': patch
---

feat(theme): add a validated DTCG token pipeline, typed `token` helper, alias-preserving CSS custom properties, and proof migrations for selected Alert and Button styles
