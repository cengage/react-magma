---
pageTitle: Tag API
title: Tag
props:
  - DeleteableTagProps, ClickableTagProps
---

<DocsHeading type="api">Tag</DocsHeading>

Styled displays of a category via search filters, metadata and other results

## Basic Usage

```typescript
import React from 'react';
import { Tag } from 'react-magma-dom';

export function Example() {
  return (
    <>
      <Tag>Text Label</Tag>
    </>
  );
}
```

## Icon Tag

```typescript
import React from 'react';
import { Tag } from 'react-magma-dom';
import { AccountCircleIcon } from 'react-magma-icons';

export function Example() {
  return (
    <>
      <Tag icon={<AccountCircleIcon />}>Icon Tag</Tag>
    </>
  );
}
```

## Clickable Tag

```typescript
import React from 'react';
import { Tag, onClick } from 'react-magma-dom';

export function Example() {
  const [counter, setCounter] = React.useState<number>(0);
  function updateCounter() {
    setCounter(count => count + 1);
  }
  return (
    <>
      <p>
        <strong>Counter: </strong> <span>{counter}</span>
      </p>
      <Tag onClick={updateCounter}>Clickable Tag</Tag>
    </>
  );
}
```

## Deletable Tag

```typescript
import React from 'react';
import { Tag, onDelete, onClick } from 'react-magma-dom';

export function Example() {
  const [isVisible, setIsVisible] = React.useState(true);

  function deleteMe() {
    setIsVisible(false);
  }
  return <>{isVisible && <Tag onDelete={deleteMe}>Deletable Tag</Tag>}</>;
}
```

## Icon With Deletable Tag

```typescript
import React from 'react';
import { Tag, onDelete, onClick } from 'react-magma-dom';
import { AccountCircleIcon } from 'react-magma-icons';

export function Example() {
  const [isVisible, setIsVisible] = React.useState(true);

  function deleteMe() {
    setIsVisible(false);
  }
  return (
    <>
      {isVisible && (
        <Tag icon={<AccountCircleIcon />} onDelete={deleteMe}>
          Deletable Tag
        </Tag>
      )}
    </>
  );
}
```

## Colors

```tsx
import React from 'react';
import { Tag, TagColor, magma } from 'react-magma-dom';

export function Example() {
  return (
    <>
      <Tag>Text Label</Tag>
      <Tag color={TagColor.primary}>Text Label Primary</Tag>
      <Tag color={TagColor.lowContrast}>Text Label Low Contrast</Tag>
      <Tag color={TagColor.highContrast}>Text Label High Contrast</Tag>
    </>
  );
}
```

## Disabled

```tsx
import React from 'react';
import { Tag, TagColor, magma } from 'react-magma-dom';

export function Example() {
  return (
    <>
      <Tag disabled>Disabled Label</Tag>
      <Tag disabled color={TagColor.lowContrast}>
        Disabled Label Low Contrast
      </Tag>
    </>
  );
}
```

## Inverse

`isInverse` is an optional boolean prop, that may be passed in when the component is to be displayed on a dark background.

```tsx
import React from 'react';
import { Card, CardBody, Tag, TagColor, magma } from 'react-magma-dom';

export function Example() {
  return (
    <Card isInverse>
      <CardBody>
        <Tag isInverse>Text Label Inverse</Tag>
        <Tag isInverse color={TagColor.primary}>
          Text Label Inverse Primary
        </Tag>
        <Tag isInverse color={TagColor.lowContrast}>
          Text Label Inverse Low Contrast
        </Tag>
        <Tag isInverse color={TagColor.highContrast}>
          Text Label Inverse High Contrast
        </Tag>
      </CardBody>
    </Card>
  );
}
```

## Inverse Disabled

```tsx
import React from 'react';
import { Card, CardBody, Tag, TagColor, magma } from 'react-magma-dom';

export function Example() {
  return (
    <Card isInverse>
      <CardBody>
        <Tag isInverse disabled>
          Disabled Label Inverse
        </Tag>
        <Tag isInverse disabled color={TagColor.lowContrast}>
          Disabled Label Inverse Low Contrast
        </Tag>
      </CardBody>
    </Card>
  );
}
```

## Internationalization

If you're using strings in your component, they must be internationalized.

Full example of <Link to="/api/internationalization">internationalization override options</Link>

```tsx
import React from 'react';
import { es } from 'date-fns/locale';
import { DatePicker, I18nContext, defaultI18n } from 'react-magma-dom';

export function Example() {
  return (
    <I18nContext.Provider
      value={{
        ...defaultI18n,
        example: 'example i18n',
      }}
    >
      <tag>override default i18n value: </tag>
    </I18nContext.Provider>
  );
}
```

# tag Props

**Any other props supplied will be provided to the wrapping `div` element.**

<DeleteableTagProps />
<ClickableTagProps />
