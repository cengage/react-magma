---
pageTitle: FormFieldContainer API
title: FormFieldContainer
props:
  - FormFieldContainerProps
---

<DocsHeading type="api">FormFieldContainer</DocsHeading>

Standard container for form field, containing a label and helper or error message

## Basic Usage

```typescript
import React from 'react';
import { FormFieldContainer } from 'react-magma-dom';

export function Example() {
  return <FormFieldContainer>FormFieldContainer content</FormFieldContainer>;
}
```

## Example

```typescript
import React from 'react';
import { FormFieldContainer, Input } from 'react-magma-dom';

export function Example() {
  return (
    <FormFieldContainer helperMessage={"Helper message"} labelText="FormFieldContainer Label">
      <Input />
    </FormFieldContainer>
  );
}
```

## Inverse

The `isInverse` prop will render a dark background and light text. The children will
inherit the `isInverse` prop unless specified otherwise.

```typescript
import React from 'react';
import { Card, CardBody, FormFieldContainer } from 'react-magma-dom';

export function Example() {
  return (
    <Card isInverse style={{ padding: '12px' }}>
      <CardBody>
        <FormFieldContainer fieldId="1" isInverse>
          FormFieldContainer content
        </FormFieldContainer>
      </CardBody>
    </Card>
  );
}
```

# FormFieldContainer Props

**Any other props supplied will be provided to the wrapping `div` element.**

<FormFieldContainerProps />
